{% extends 'base.html.twig' %}
{% trans_default_domain 'article' %}

{% block body %}
    <h1 class="title-blog">{{ 'article.creation' | trans }}</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input type="submit" value={{ 'article.submit'|trans }} />
    {{ form_end(form) }}

    <ul>
        <li>
            <a href="{{ path('admin_index') }}">Back to the list</a>
        </li>
    </ul>
{% endblock %}
